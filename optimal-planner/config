# config.py

# Parámetros del velero
MAX_WIND_ANGLE = 45  # Grados mínimo para navegar en contra del viento (zona prohibida)
SAIL_SPEED_KNOTS = 5  # Velocidad estándar del velero

# Archivos de entrada
WAYPOINTS_FILE = "data/waypoints.csv"

# Otras configuraciones futuras (para crecer el sistema)
USE_WIND_DATA = False
